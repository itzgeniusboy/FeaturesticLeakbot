name: 24x7 Telegram Bot Loop

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:
  schedule:
    - cron: '0 */5 * * *' # Backup trigger agar kabhi loop tute

jobs:
  run-bot-job:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Setup Python environment
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install PyTelegramBotAPI
      run: |
        python -m pip install --upgrade pip
        pip install pyTelegramBotAPI

    - name: Run Main Bot Script
      env:
        BOT_TOKEN: "8775696477:AAEnN0Onmw09_m3UJBLPyGCg389yLR4LReI"
        CHANNEL_ID: "-1003201511422"
      run: |
        python bot.py

    - name: Trigger Next Loop (24x7 Continuous)
      if: always() # Bot crash ho ya time khatam ho, naya loop start ho jayega
      run: |
        curl -X POST \
          -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
          -H "Accept: application/vnd.github.v3+json" \
          https://api.github.com/repos/${{ github.repository }}/actions/workflows/run.yml/dispatches \
          -d '{"ref":"${{ github.ref_name }}"}'
